# KSS Connect — School Management System

ระบบจัดการโรงเรียนแบบสมบูรณ์ (Next.js 16 + React 19 + Tailwind CSS 4 + SQLite)

## ภาพรวมสัญญา

🎓 ระบบบริหารจัดการโรงเรียนครบวงจร รองรับนักเรียน ครู และแอดมิน

### ฟีเจอร์หลัก
- ✅ **ระบบลงชื่อเข้าใช้** สำหรับนักเรียน ครู และแอดมิน
- ✅ **แอดมินแพนเนล** จัดการนักเรียน ครู ประกาศ กิจกรรม รายงาน ฯลฯ
- ✅ **ระบบข้อความ** ส่งข้อความมิติเดียวระหว่างนักเรียน
- ✅ **แอดมินตรวจสอบการลงชื่อ** ระบบรีโพร์ต
- ✅ **AI Chatbot** ใช้ Groq API สำหรับตอบคำถาม
- ✅ **Responsive Design** รองรับมือถืออย่างสมบูรณ์

## เทคโนโลยี
- **Frontend**: Next.js 16.1.1, React 19, Tailwind CSS 4
- **Database**: SQLite (better-sqlite3)
- **Authentication**: bcryptjs, cookies
- **Validation**: Zod
- **UI Components**: SweetAlert2
- **AI**: Groq API (@google/generative-ai)

## Requirements
- Node.js 18+
- npm หรือ yarn
- Windows/Mac/Linux

## เริ่มต้นแบบโลคัล

```bash
# 1. ติดตั้ง dependencies
npm install

# 2. เริ่มตัวพัฒนา server
npm run dev

# 3. เปิด browser
http://localhost:3000
```

## Build สำหรับ Production

```bash
npm run build
npm run start
```

## สร้างบัญชีทดสอบ

```bash
# สร้างแอดมิน
node scripts/create-admin.js admin123 password123

# สร้างนักเรียน
node scripts/create-student.js สมชาย ใจดี ม.1/1 password123

# สร้างครู
node scripts/create-teacher.js นาย สมดี password123
```

## โครงสร้างไฟล์

```
app/
├── page.tsx              # หน้าเข้าสู่ระบบนักเรียน
├── dashboard/            # แดชบอร์ดนักเรียน
├── admin/                # แอดมินแพนเนล
├── teacher/              # ครูแพนเนล
├── messages/             # ระบบข้อความ
├── api/                  # API routes
└── components/           # React components

lib/
└── db.ts                 # SQLite database functions

scripts/
├── create-admin.js       # สร้างแอดมิน
├── create-student.js     # สร้างนักเรียน
└── create-teacher.js     # สร้างครู

public/
└── uploads/              # ที่เก็บไฟล์อัปโหลด
```

## Environment Variables

สร้าง `.env.local` (สำหรับ local development):

```env
GROQ_API_KEY=your_groq_api_key_here
```

สำหรับ Vercel ให้ดูที่ `.env.example`

## Deployment on Vercel

1. Push code ไปที่ GitHub
2. ไปที่ [Vercel Dashboard](https://vercel.com)
3. Import โปรเจกต์ จาก GitHub
4. ตั้งค่า Environment Variables:
   - `GROQ_API_KEY` (optional)
5. Deploy!

## Database Notes

- SQLite database จะสร้างอัตโนมัติที่ `data/kss.db`
- ทุก API routes มี `runtime = 'nodejs'` เพื่อรองรับ better-sqlite3
- สำหรับ Vercel: ฐานข้อมูลจะสร้างใหม่ทุกครั้งที่ redeploy

## API Routes

### Student Routes
- `POST /api/login` - เข้าสู่ระบบ
- `POST /api/register` - สมัครสมาชิก
- `GET /api/messages` - ดูข้อความ
- `POST /api/messages` - ส่งข้อความ
- `GET /api/events` - ดูกิจกรรม

### Admin Routes
- `POST /api/admin/login` - เข้าสู่ระบบแอดมิน
- `GET/POST /api/admin/users` - จัดการนักเรียน
- `GET/POST /api/admin/announcements` - ประกาศ
- `GET/POST /api/admin/events` - กิจกรรม
- `GET /api/admin/reports` - รีโพร์ต

## หมายเหตุสำหรับ Production

- ✅ Tailwind CSS v4 พร้อม JIT compilation
- ✅ Image optimization พร้อม unoptimized flag สำหรับ SQLite compatibility
- ✅ SWC minification เปิดใช้งาน
- ✅ All inputs validated with Zod
- ✅ CORS และ security headers ตั้งค่าแล้ว

## License

MIT

## Support

สำหรับปัญหา ให้ติดต่อทีมพัฒนา
